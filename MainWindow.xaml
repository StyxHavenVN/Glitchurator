<Window x:Class="StandalonePicturator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:StandalonePicturator.View.SliderPicturator"
        xmlns:tick="clr-namespace:StandalonePicturator.View.TickArt"
        xmlns:analyzer="clr-namespace:StandalonePicturator.View.SliderAnalyzer"
        xmlns:extreme="clr-namespace:StandalonePicturator.View.ExtremeSlider"

        mc:Ignorable="d"
        Title="Glitchurator — Styx (StyxHavenVN)" Height="820" Width="1380" MinWidth="1100" MinHeight="650"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E1E">
    
    <Window.Resources>
        <!-- Style cho TabItem: Nền sáng, viền bo tròn, chữ màu đen đậm -->
        <Style TargetType="TabItem">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="Margin" Value="0,0,4,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border Name="Border" Background="#E0E0E0" CornerRadius="4,4,0,0" Padding="{TemplateBinding Padding}" Margin="{TemplateBinding Margin}">
                            <ContentPresenter x:Name="ContentSite" VerticalAlignment="Center" HorizontalAlignment="Center" ContentSource="Header" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#FFFFFF" />
                                <Setter Property="Foreground" Value="#1E1E1E" />
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#F5F5F5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="8">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <DockPanel Margin="4,0,4,8">
            <Button DockPanel.Dock="Right" Content="Choose map…" Command="{Binding SelectBeatmapCommand}"/>
            <TextBlock Text="Shared beatmap .osu" Foreground="White" VerticalAlignment="Center" Margin="4,0,12,0"/>
            <TextBox Text="{Binding BeatmapPath, UpdateSourceTrigger=LostFocus}"/>
        </DockPanel>
        <TabControl Grid.Row="1" Background="#1E1E1E" BorderThickness="0">
            <!-- Tab 1: Công cụ vẽ ảnh Picturator -->
            <TabItem Header="Slider Picturator">
                <views:SliderPicturatorView x:Name="Picturator" />
            </TabItem>
            
            <!-- Tab 2: Công cụ phân tích và tái tạo -->
            <TabItem Header="Sliderball"><views:SliderballView /></TabItem>
            <TabItem Header="Slider Analyzer &amp; Re-Picturator">
                <analyzer:SliderAnalyzerView x:Name="Analyzer" />
            </TabItem>


        </TabControl>
        <TextBlock Grid.Row="2" Text="Mapping Tools extension for Slider Picturator &amp; Sliderball • Styx (StyxHavenVN) • Copying and sharing welcome." Foreground="#A0ADB8" FontSize="11" HorizontalAlignment="Right" Margin="8,6,8,0"/>
    </Grid>
</Window>



